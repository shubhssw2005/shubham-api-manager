{{- if .Values.serviceMonitors.api-service.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-service-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    app: api-service
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      {{- toYaml .Values.serviceMonitors.api-service.selector.matchLabels | nindent 6 }}
  endpoints:
    {{- range .Values.serviceMonitors.api-service.endpoints }}
    - port: {{ .port }}
      interval: {{ .interval }}
      path: {{ .path }}
      scheme: http
      honorLabels: true
      metricRelabelings:
        # Add cluster label
        - sourceLabels: []
          targetLabel: cluster
          replacement: {{ $.Values.global.environment }}
        # Add service label
        - sourceLabels: []
          targetLabel: service
          replacement: api-service
        # Relabel tenant_id to tenant for consistency
        - sourceLabels: [tenant_id]
          targetLabel: tenant
          replacement: ${1}
    {{- end }}
  namespaceSelector:
    matchNames:
      - default
{{- end }}