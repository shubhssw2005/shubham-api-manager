{{- if .Values.autoscaling.verticalPodAutoscaler.enabled }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "api-service.fullname" . }}-vpa
  labels:
    {{- include "api-service.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "api-service.fullname" . }}
  updatePolicy:
    updateMode: {{ .Values.autoscaling.verticalPodAutoscaler.updateMode | default "Auto" }}
  resourcePolicy:
    containerPolicies:
    - containerName: {{ .Chart.Name }}
      minAllowed:
        cpu: {{ .Values.autoscaling.verticalPodAutoscaler.minAllowed.cpu | default "100m" }}
        memory: {{ .Values.autoscaling.verticalPodAutoscaler.minAllowed.memory | default "128Mi" }}
      maxAllowed:
        cpu: {{ .Values.autoscaling.verticalPodAutoscaler.maxAllowed.cpu | default "2000m" }}
        memory: {{ .Values.autoscaling.verticalPodAutoscaler.maxAllowed.memory | default "4Gi" }}
      controlledResources: ["cpu", "memory"]
      controlledValues: RequestsAndLimits
{{- end }}